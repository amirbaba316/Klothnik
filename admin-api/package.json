{
    "name": "@klothnick/admin-api",
    "version": "1.0.0",
    "main": "app.js",
    "scripts": {
        "dev": "ts-node-dev --conditions=development --respawn --pretty --transpile-only app.ts",
        "build": "npx tsc --build",
        "bundle": "node esbuild.config.js",
        "start": "node build/app.js",
        "test": "jest",
        "format": "prettier --write --ignore-unknown src/"
    },
    "dependencies": {
        "@klothnick/shared": "*",
        "express": "^4.18.2",
        "express-async-errors": "^3.1.1",
        "mongoose": "^7.6.1",
        "morgan": "^1.10.1"
    }
}
